    <h2>Welcome to Doodle!</h2>
<section class="row">
  <div class="column createnew">
    {{log drawings}}
    <h2>Create New Drawing</h2>

    <form class="new-drawing-form">
      <div>
        <label for="drawing-title"> Title:
          <input type="text" id="drawing-title" name="drawing-title" />
        </label>
      </div>
      <div>
        <label for="drawing-description"> Drescription:
          <input type="text" id="drawing-description" name="drawing-description" />
        </label>
      </div>
      <div>
        <button type="submit" class="action dashbtn">Create</button>
      </div>
    </form>
  </div>

</section>
{{log drawings}}
{{#if drawings}}
<section class="your-drawings">
  <h2>Your Drawings</h2>
  <ul>
    {{#each drawings as |drawings|}}
    <li>
      {{!-- {{> post-info post}} --}}
      <a class="edit-link" href="#modal-full" uk-toggle>{{drawings.name}}</a>

      <div id="modal-full" class="uk-modal-full" uk-modal>
        <div class="uk-modal-dialog dashview">
          <h2 class="uk-modal-title">{{drawings.name}}</h2>
          <button class="uk-modal-close-full uk-close-large" type="button" uk-close></button>
          <div class="uk-grid-collapse uk-child-width-1-2@s uk-flex-middle" uk-grid>
            <div class="uk-background-cover" uk-height-viewport>
              <p>Options:
                <button class="action">Edit</button>
                <button class="action" data-id="{{drawings.id}}">DELETE</button>
                <button class="action">Downlaod</button>
              </p>
            </div>
            <div class="uk-padding-large">
              <img src="https://plchldr.co/i/500x250">
            </div>
          </div>
        </div>
      </div>

    </li>
    {{/each}}
  </ul>

</section>
{{/if}}

<!--Script to add a post-->
<script src="/js/add-drawing.js"></script>