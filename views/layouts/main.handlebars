<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Drawings Creator and Gallery</title>
  <link rel="stylesheet" href="/css/1.3.0/css/line-awesome.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="page-container flex-column justify-space-around">
    <div id="content-wrapper">
      <header class="display-flex justify-space-between align-center p-2">
        <h1>
          <a class="no-button" href="/">Drawings Creator</a>
        </h1>
        <nav>
          <span><a class="no-button" href="/">Home</a></span> |
          <!-- Conditionally render login or logout links -->
          {{#if logged_in}}
          <a class="no-button" href="/dashboard">Dashboard</a> |
          <button class="no-button" id="logout">Logout</button>
          {{else}}
          <a href="/login">Login</a>
          {{/if}}
        </nav>
      </header>
      <main class="container container-fluid mt-5">
        <!-- Render the sub layout -->
        {{{ body }}}
      </main>
      <footer id="footer">
        <p class="no-button">Thanks for drawing with us!</p>
      </footer>
    </div>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>